# Cloudflare R2 CDN URL for data storage
# This should point to your R2 bucket where processed JSON data is stored
NEXT_PUBLIC_DATA_URL=https://pub-your-bucket-id.r2.dev

# Mapbox Access Token for map visualizations
# Get your token from https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here

# Optional: Analytics (if needed in future)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Optional: API endpoints (if backend is added)
# NEXT_PUBLIC_API_URL=https://api.example.com